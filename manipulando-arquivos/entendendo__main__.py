import extraindo_nomes_lista as enl

print(__name__)  # mostra o nome atribuido a função principal do modulo
print(enl.__name__) # mostra o nome do módulo em

# if __name__ == "__main__" só está checando se este módulo é o programa principal.
